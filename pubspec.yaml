name: architect_portfolio
description: Flutter Frontend | Golang Backend Portfolio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  url_launcher: ^6.2.4
  visibility_detector: ^0.4.0+2
  flutter_animate: ^4.5.0
  url_launcher_ios: ^6.4.1
  firebase_core: ^4.7.0
  firebase_analytics: ^12.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
